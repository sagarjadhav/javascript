/**
 * The memory heap is a place to store and write information so that we can use our memory appropriately.
 * It is a place to allocate, use, and remove memory as needed.
 * Think of it as a storage room of boxes that are unordered.
 */

// Tell the memory heap to allocate memory for a number.
const number = 11;

// Allocate memory for a string.
const string = "some text";

// Allocate memory for an object and it's values.
const person = {
	first: "Brittney",
	last: "Postma"
};
